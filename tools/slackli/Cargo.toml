[package]
name = "slackli"
version = "0.1.0"
edition = "2021"
description = "Agent-safe Slack CLI foundation"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
assert_cmd = "2"
predicates = "3"
